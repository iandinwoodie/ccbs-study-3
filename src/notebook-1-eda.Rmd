---
title: "Exploratory Data Analysis"
date: "`r Sys.Date()`"
output:
  html_document: default
  pdf_document: default
---

```{r setup, include=FALSE}
library(tidyverse)
knitr::opts_chunk$set(echo=TRUE)
```

# Overview

This notebook catalogs the exploratory data analysis undertaken for this
project.

# Loading the Data

If the tidy dataset does not exist, then build it.

```{r}
tidy_data_filepath <- '../data/tidy.Rds'
df_owners <- read.csv('../data/raw-owners.csv', header=TRUE, skipNul=TRUE)
names(df_owners)[names(df_owners) == 'What.is.your.email.'] <- 'owner_id'
stopifnot(identical(dim(df_owners)+0, c(717, 6)))
str(df_owners)
```

